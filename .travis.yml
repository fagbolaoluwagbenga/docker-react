sudo: required
services: 
  - docker

before_install:
  - docker build -t oluwagbenga/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true oluwagbenga/docker-react npm run test -- --coverage  